# プライバシーポリシー

**対象**: Chrome / Edge 拡張機能「note to Markdown」（以下「本拡張機能」）
**最終更新日**: 2026年9月17日

## 収集する情報

**本拡張機能は、利用者の個人情報を含むいかなるデータも収集・送信しません。**

開発者が運営するサーバーはありません。解析ツール、広告、トラッキング、クラッシュレポートの類も一切組み込んでいません。

## 端末内に保存する情報

本拡張機能は、以下の設定を**利用者の端末内にのみ**保存します。外部に送信されることはありません。

| 保存先 | 内容 |
|--------|------|
| `chrome.storage.local` | 変換元・変換後の選択、入力した記事URL、選択中のタグ、タグ候補、タグセットプリセット、保存先プリセット名、画像取込方式、Obsidianリンク化の設定とワード一覧、表示言語の設定 |
| IndexedDB | 利用者が選択した保存先フォルダへのアクセスハンドル（File System Access API） |

これらは拡張機能をアンインストールすると削除されます。オプション画面から個別に解除することもできます。

## 設定のインポート / エクスポート

オプション画面の「設定のインポート / エクスポート」は、タグ候補・タグセットプリセット・Obsidianリンク化の設定を JSON ファイルとして利用者の端末へ書き出し、また読み込む機能です。ファイルの保存先と受け渡しは利用者が管理するもので、本拡張機能が外部へ送信することはありません。保存先フォルダへのアクセスハンドルは書き出しの対象に含まれません。

## 通信

本拡張機能が通信を行うのは、利用者が変換またはスキ数の更新を実行したときの以下だけです。

| 接続先 | 目的 |
|--------|------|
| `https://note.com` | 変換対象として指定された記事のHTMLを取得する |
| `https://note.com/api/v3/notes/` | 「スキ数を更新」を実行した場合に、対象記事の現在のスキ数を取得する |
| `https://assets.st-note.com` | 「画像ダウンロード」「Base64埋込」を選択している場合に、記事内の画像を取得する |

いずれも記事の情報を取得するだけで、利用者の情報を送信するものではありません。認証情報を伴わない形（`credentials: "omit"`）で取得しているため、note.com にログインしていても、そのアカウント情報が送られることはありません。

## ファイルへのアクセス

利用者がオプション画面で明示的に選択したフォルダに対してのみ、以下の読み書きを行います。選択されていないフォルダやファイルへアクセスすることはありません。

| 機能 | 動作 |
|------|------|
| ダウンロード | 選択されたフォルダへ `.md` ファイルと画像を書き込む |
| スキ数を更新 | 選択されたフォルダ配下の `.md` を読み取り、frontmatter の `like_count` だけを書き換える |

「スキ数を更新」は既存ファイルを上書きします。実行前に対象件数を表示して確認を求めますが、書き換えを取り消す機能はありません。

## 権限の用途

| 権限 | 用途 |
|------|------|
| `activeTab` | 拡張機能アイコンを操作したときに、開いている note 記事タブの内容を変換するため |
| `clipboardWrite` | 変換した Markdown をクリップボードへコピーするため |
| `storage` | 上記の設定を端末内に保存するため |
| `sidePanel` | 変換画面をブラウザのサイドパネルに表示するため（画面表示のみで、データへのアクセスは伴いません） |
| `https://note.com/*` | 記事の取得・変換、および「スキ数を更新」でのスキ数の取得のため |
| `https://assets.st-note.com/*` | 記事内画像の取得のため |

## 第三者提供

収集するデータが存在しないため、第三者への提供・販売・共有は一切ありません。

## 変更について

本ポリシーを変更する場合は、本ファイルを更新し、拡張機能の更新履歴（[CHANGELOG.md](CHANGELOG.md)）に記載します。

## お問い合わせ

本拡張機能に関するお問い合わせは、GitHub リポジトリの Issue からお願いします。

---

本拡張機能は note株式会社とは関係のない非公式ツールです。
