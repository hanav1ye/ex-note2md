# 更新履歴

利用者に見える変化を版ごとにまとめる。ストアの「更新内容」欄には `docs/store-listing.md` の要約を使う。

## 1.1.0 — 2026-09-17

### 追加

- **UI の英語表示**。設定画面の「表示言語」で 自動 / 日本語 / English を切り替えられる。既定はブラウザの表示言語に追従。popup・設定画面・note ページ上の通知・Service Worker のエラー文言すべてに適用される。Markdown の出力内容は変わらない
- **設定のインポート / エクスポート**。タグ候補・タグセットプリセット・Obsidian リンク化設定を JSON で書き出し、別の端末で取り込める。取り込みはマージのみで、既存の登録は消えない。保存先フォルダは対象外
- **スキ数を更新**。保存先プリセットのフォルダ配下にある変換済み `.md` の `like_count` を、note の最新値へ一括更新する。設定画面と popup / サイドパネルのハートアイコンから実行。実行前に対象件数を確認し、値が同じファイルは書き込まない
- **サイドパネルに常設**。popup 右上の「サイドバーで開く」で同じ画面をブラウザのサイドパネルに開く。ページをクリックしても閉じず、タブを移動すると処理対象が追従する
- **保存先プリセットを 3 → 5 に拡張**
- ストア掲載用のプロモーションタイルを生成する `npm run make:store-images`
- `npm run build:dist` が提出用の `ex-note2md-{version}.zip` まで生成する

### 変更

- popup の並びを「変換元 → 変換後 → タグ」に変更
- 掲載スクリーンショットを 640x400 → 1280x800 に
- **対応バージョンを Chrome / Edge 116 以降に変更**（Side Panel API のため。従来は 109 以降）
- 拡張機能名と説明を `_locales` でローカライズ

### 修正

- マガジンページで複数選択すると記事名がすべて「（タイトル不明）」になっていた。カード内の見出しからタイトルを拾う
- マガジンページで「一覧を全選択」が 0 件になっていた。一覧コンテナの探し方をページ構造に依存しない形に変更
- プリセット名を編集すると既定値まで書き換わっていた（既定値オブジェクトを使い回していた）

### プライバシーポリシーの変更

- 通信先に `https://note.com/api/v3/notes/`（スキ数の取得）を追加。認証情報は送らない
- ファイルへのアクセスに「スキ数を更新」（`.md` の読み取りと `like_count` の書き換え）を追加
- 権限に `sidePanel` を追加（画面表示のみ）
- 端末内に保存する情報に「表示言語の設定」を追加

## 1.0.1 — 2026-08-11

### 変更

- ビルド時に `manifest_version` と `version` の形式を検査するように。`dist/` を手で編集して壊れた状態のまま提出することがあったため

## 1.0.0 — 2026-08-03

初回リリース。

- note 記事ページ、または記事 URL から Markdown を生成
- クリップボードへコピー、またはローカルフォルダへ `.md` 保存（保存先プリセット 3 つ）
- 画像は note URL 参照 / ローカル保存 / Base64 埋込から選択
- note ページ上の記事リンクを単体選択 / 複数選択 / 一覧を全選択
- タグ候補とタグセットプリセット
- Obsidian 向けリンク化（指定ワードを `[[単語]]` 化）
